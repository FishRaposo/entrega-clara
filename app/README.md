# Application

All application source code lives beneath this directory. `backend/` and `web/` are deliberately separate deployable surfaces that share the repository-level contracts documented at the root.
